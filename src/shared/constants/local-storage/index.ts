const PREFIX_KEY = 'FF';

export const LS_AUTH_KEY = `${PREFIX_KEY}=auth`;
export const LS_THEME_KEY = `${PREFIX_KEY}=theme`;

export const LS_ARTICLES_VIEW_KEY = `${PREFIX_KEY}=articles__view`;
export const LS_ARTICLES_STATIC_SCROLLED_HEIGHT = `${PREFIX_KEY}=articles__static-scrolled-height`;
export const LS_ARTICLES_VIRTUOSO_SCROLL_INDEX = `${PREFIX_KEY}=articles__virtuoso-scroll-index`;
